# Config settings
title: Vikitravels slide testing
baseurl: "/jekyll-theme-vikitravels"

# Build settings
markdown: kramdown
#theme: minima
theme: jekyll-theme-chirpy
plugins:
  - jekyll-feed
exclude:
  - Gemfile
  - Gemfile.lock
